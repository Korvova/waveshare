# Waveshare RP2350-POE-ETH-8DI-8RO - HTTP Relay Control

Веб-сервер на MicroPython для управления 8 реле через HTTP и REST API.

## ✨ Возможности

- 🌐 HTTP веб-интерфейс для управления реле
- 🔌 REST API для автоматизации
- 🔄 8 реле (GPIO 17-24)
- 📡 Ethernet W5500 (статический IP)
- 🚀 Автозапуск при загрузке
- 📱 Адаптивный дизайн

## 🎯 Быстрый старт

### 1. Подготовка платы

**Скачайте специальную прошивку Waveshare:**
```bash
# Скачайте с сайта Waveshare или из репозитория
https://files.waveshare.com/wiki/common/WAVESHARE_RP2350B.zip
```

**Установите прошивку:**
1. Зажмите кнопку **BOOTSEL** на плате
2. Подключите USB к компьютеру (держа BOOTSEL)
3. Отпустите BOOTSEL - появится диск (D: или E:)
4. Скопируйте `WAVESHARE_RP2350_ETH_8DI_8RO.uf2` на диск
5. Плата автоматически перезагрузится

### 2. Установите mpremote

```bash
pip install mpremote
```

### 3. Загрузите веб-сервер на плату

```bash
# Подключитесь к плате (замените COM6 на ваш порт)
mpremote connect COM6 fs cp main_waveshare.py :boot.py

# Перезагрузите плату
mpremote connect COM6 reset
```

### 4. Подключите Ethernet

1. Подключите **Ethernet кабель** от платы к компьютеру
2. На компьютере настройте статический IP на Ethernet адаптере:
   - IP: `192.168.1.10`
   - Маска: `255.255.255.0`
   - Шлюз: `192.168.1.1`

### 5. Откройте браузер

```
http://192.168.1.100
```

## 🔧 Конфигурация

По умолчанию в `main_waveshare.py`:

```python
STATIC_IP = '192.168.1.100'
SUBNET = '255.255.255.0'
GATEWAY = '192.168.1.1'
DNS = '8.8.8.8'
```

Измените эти значения если нужен другой IP адрес.

## 📡 REST API

### Получить состояние всех реле
```bash
curl http://192.168.1.100/api/relays
```

Ответ:
```json
{
  "relay_1": {"state": 0},
  "relay_2": {"state": 1},
  ...
  "relay_8": {"state": 0}
}
```

### Управление одним реле
```bash
# Включить реле 1
curl -X POST http://192.168.1.100/api/relay/1 \
  -H "Content-Type: application/json" \
  -d '{"state": 1}'

# Выключить реле 1
curl -X POST http://192.168.1.100/api/relay/1 \
  -H "Content-Type: application/json" \
  -d '{"state": 0}'
```

### Управление всеми реле
```bash
# Включить все
curl -X POST http://192.168.1.100/api/relays/all/on

# Выключить все
curl -X POST http://192.168.1.100/api/relays/all/off
```

## 🗂 Структура проекта

```
waveshare/
├── main_waveshare.py      # Основной веб-сервер (загружать как boot.py)
├── test_eth.py            # Тест Ethernet (для отладки)
├── README.md              # Этот файл
├── .gitignore             # Git ignore
└── c/                     # C версия (альтернатива)
    └── web_server/
        ├── main.c
        ├── config.h
        └── web_pages.h
```

## 🐛 Решение проблем

### Плата не отвечает по сети

1. **Проверьте Ethernet кабель:**
   - Плотно ли вставлен в плату и компьютер?
   - Мигают ли индикаторы на порту?

2. **Проверьте сеть на компьютере:**
   ```bash
   ipconfig  # Windows
   ifconfig  # Linux/Mac
   ```
   Убедитесь что адаптер имеет IP `192.168.1.10`

3. **Проверьте подключение:**
   ```bash
   # Ping может не работать (ICMP), но TCP работает:
   curl -v http://192.168.1.100
   ```

### COM порт занят

Закройте все программы, использующие COM порт:
- Thonny IDE
- PuTTY
- Другие serial терминалы
- VS Code serial monitor

### Реле не щелкают

```bash
# Проверьте через mpremote:
mpremote connect COM6 exec "
from machine import Pin
relay = Pin(17, Pin.OUT)
relay.value(1)  # Должно щелкнуть
"
```

## 📝 Важные заметки

1. **Используйте только прошивку Waveshare** `WAVESHARE_RP2350_ETH_8DI_8RO.uf2`
   - Стандартная MicroPython не поддерживает GPIO 34-36 (нужны для W5500)
   - Прошивка Waveshare включает встроенный модуль `network.WIZNET5K`

2. **Ping может не работать:**
   - W5500 или firewall могут блокировать ICMP
   - HTTP (TCP) работает отлично

3. **Автозапуск:**
   - Файл `boot.py` запускается автоматически при загрузке
   - Если нужно остановить сервер: `Ctrl+C` через serial

## 🔗 Полезные ссылки

- [Waveshare Wiki](https://www.waveshare.com/wiki/RP2350-ETH-8DI-8RO)
- [MicroPython Docs](https://docs.micropython.org/)
- [Схема платы](https://files.waveshare.com/wiki/RP2350-ETH-8DI-8RO/RP2350-ETH-8DI-8RO-Sch.pdf)

## 📄 Лицензия

MIT License

## 🤝 Автор

Создано с помощью Claude Code
